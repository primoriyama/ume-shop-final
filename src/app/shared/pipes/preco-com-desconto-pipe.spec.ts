import { PrecoComDescontoPipe } from './preco-com-desconto-pipe';

describe('PrecoComDescontoPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecoComDescontoPipe();
    expect(pipe).toBeTruthy();
  });
});
